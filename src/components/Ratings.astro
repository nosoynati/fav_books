---
import Stars from './Stars.astro';
const { rating } = Astro.props;
const total = 5;
---
<div style="display:flex; gap:4px">
  {Array.from({ length: total }).map((_, i) => {
    const filled = Math.min(1, Math.max(0, rating - i));
    return <Stars filled={filled} index={i} />;
  })}
</div>